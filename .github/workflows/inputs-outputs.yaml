name: "handling inputs and outputs in github action workflows"

on:
  workflow_dispatch:

jobs:
  call-reusable:
    runs-on: "ubuntu-latest"
    steps:
      - name: "using inputs"
        uses: "./.github/workflows/reusable.yaml@main"
        with:
          name: "Imran"
